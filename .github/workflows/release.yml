name: Release

# Manually triggered from GitHub UI: builds the APK, tags the commit,
# and publishes a GitHub Release with the APK attached.
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version, without the leading v (e.g. 1.0.0)'
        required: true

permissions:
  contents: write   # create the tag and the release

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history + tags for changelog

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run unit tests
        run: ./gradlew testDebugUnitTest --no-daemon

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon

      - name: Stage APK
        run: |
          mkdir -p release
          cp app/build/outputs/apk/debug/app-debug.apk \
             "release/doc-scanner-${{ github.event.inputs.version }}.apk"

      - name: Create tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ github.event.inputs.version }}" -m "Release v${{ github.event.inputs.version }}"
          git push origin "v${{ github.event.inputs.version }}"

      - name: Generate release notes
        run: |
          version="v${{ github.event.inputs.version }}"
          prev_tag="$(git describe --tags --abbrev=0 "${version}^" 2>/dev/null || true)"
          {
            echo "## What's Changed"
            echo
            if [ -n "$prev_tag" ]; then
              git log --no-merges --pretty='- %s' "${prev_tag}..${version}"
              echo
              echo "**Full Changelog**: https://github.com/${{ github.repository }}/compare/${prev_tag}...${version}"
            else
              git log --no-merges --pretty='- %s' "${version}"
            fi
          } > release/notes.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v${{ github.event.inputs.version }}"
          name: "v${{ github.event.inputs.version }}"
          body_path: release/notes.md
          files: release/doc-scanner-${{ github.event.inputs.version }}.apk

# Built outputs
build/
.gradle/
*.apk
*.aab
*.aar

# Local config (contains SDK path — machine-specific)
local.properties

# Release keystore — never commit, store as GitHub Secret
keystore/
*.jks

# IDE
.idea/
*.iml
*.iws
*.ipr

# Claude AI workspace — commit rules and settings, ignore local overrides
.claude/settings.local.json
.claude/worktrees/

# OS
.DS_Store
Thumbs.db

# Kotlin scratch files
*.scratch

# Test reports
app/build/
